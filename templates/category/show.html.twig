{% extends 'base.html.twig' %}

{% block title %}{{ category.name }}{% endblock %}

{% block body %}
    <section class="container">
        <h1>{{ category.name }}</h1>
        <hr>
        {% include 'article/_list.html.twig' with { articles } %}
    </section>
{% endblock %}
